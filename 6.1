#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Podaj liczbę n: ";
    cin >> n;

    int count = 0;
    for (int i = 1; i <= n; ++i) {
        if (i % 5 == 0 && i % 3 != 0) {
            count++;
        }
    }

    cout << "Ilość liczb naturalnych nie większych od " << n << ", które są podzielne przez 5, ale nie przez 3 to: " << count << endl;
    return 0;
}
